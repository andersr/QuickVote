QV = {

  checkForWinner: function(voteId){


    //only call if hasVotes
    //get voteChoices with this id
    //get the count for each voteChoice
    // get winningCount?
    // if count for these voteChoices is higher, replace winningVotes with this one, else push equal value votes onto winningVotes 

  }

};